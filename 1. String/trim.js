/**
 * Trim is used to trim remove white space from the starting and ending of a string
 * Trim doesnot trim white space in between the words
 * When we use trim method the original string remain same and trim method return a new modified string without space
 */


let str = " Hello ";
let modstr = str.trim();
console.log(str.trim());